import { jsx as _jsx } from "react/jsx-runtime";
import FastImage from 'react-native-fast-image';
import { useSolitoImage } from '../use-solito-image';
export default function SolitoFastImage(props) {
    const { source, resizeMode, onLoadingComplete, style, ...imageProps } = useSolitoImage(props);
    return (_jsx(FastImage, { source: source && typeof source == 'object' && 'uri' in source
            ? {
                priority: props.priority ? 'high' : 'normal',
                cache: 'web',
                headers: source.headers,
                uri: source.uri,
            }
            : source, resizeMode: props.resizeMode, onLoad: onLoadingComplete && ((e) => onLoadingComplete(e.nativeEvent)), 
        // @ts-expect-error this is fine
        style: style, ...imageProps }, void 0));
}
//# sourceMappingURL=fast.js.map