import React from 'react';
import { MiddlewareContextType } from './types';
declare type Props = {
    middleware: MiddlewareContextType;
};
export declare function SolitoProvider({ children, middleware, }: {
    children: React.ReactNode;
} & Props): JSX.Element;
export {};
